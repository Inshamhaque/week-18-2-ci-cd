function firstel(input:(number|string)[]){
    return input[0];
}
const val = firstEl([1,"Insham","Haque"]);
// val.toUpperCase();
//1-> this does not exists on the number type and so shall not exist on this input type. 
